<resources>

    <!-- Button -->
    <style name="Button" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="Button.Response" parent="Button">
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/button</item>
    </style>

    <style name="Login"/>

    <style name="Login.EditText">
        <item name="android:colorActivatedHighlight">@color/textNormal</item>
        <item name="colorControlNormal">@color/textNormal</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:textColor">@color/textNormal</item>
        <item name="android:textColorHint">@color/textUnselected</item>
        <item name="android:textSize">18sp</item>
    </style>

</resources>
